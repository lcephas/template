# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
# azure.yaml is an azd configuration file and isn't used by the sample application. 

name: tomcat-app-service-mysql-redis-infra
metadata:
  template: tomcat-app-service-mysql-redis-infra@0.0.1-beta
# services:
#   web:
#     project: .
#     language: java
#     host: appservice
hooks:
  postprovision:
    posix: 
      shell: sh
      run: echo 'App Insights connection string:' && echo "$APPLICATIONINSIGHTS_CONNECTION_STRING"
      interactive: true
      continueOnError: true
